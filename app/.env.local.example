# 5b — the active TegPool tournament the lobby talks to.
# Celo Sepolia is chainId 11142220. Redeploy a fresh short-timer instance per test
# (see contracts/DEPLOY.md) and paste its addresses here in app/.env.local (gitignored).
NEXT_PUBLIC_CHAIN_ID=11142220
NEXT_PUBLIC_TEGPOOL_ADDRESS=0x...
NEXT_PUBLIC_USDT_ADDRESS=0x...

# Backend (Plan 5c) — server-only, NEVER commit real values
DATABASE_URL=postgres://user:pass@host/db
CRON_SECRET=choose-a-long-random-string
ORACLE_PRIVATE_KEY=0x...        # the oracle hot wallet (testnet only here)
# RPC_URL=https://...           # optional: dedicated RPC (public forno can 403 under load)
