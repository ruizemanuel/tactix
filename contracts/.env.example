DEPLOYER_PRIVATE_KEY=0xyour_funded_testnet_deployer_key
CELOSCAN_API_KEY=your_etherscan_v2_api_key
# Required for a mainnet deploy (immutable in TegPool). Optional on testnet/local (defaults to deployer).
# PLATFORM_WALLET=0xyour_platform_fee_wallet
# Required for a mainnet deploy: the contract owner (a multisig/timelock — controls fees/oracle/pause/emergency).
# Optional on testnet/local (defaults to the deployer).
# OWNER=0xyour_multisig_or_timelock
